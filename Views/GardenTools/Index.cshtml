@model IEnumerable<PlantTracker3NET.Models.GardenTool>
@{ Layout = null; }
<!DOCTYPE html>
<html>
<head>
    <title>Garden Tools</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
<div class="container mt-5">
    <div class="card shadow-sm p-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="m-0">Garden Tools</h2>
            <div>
                <a href="/Home/Index" class="btn btn-outline-secondary">Back to Menu</a>
                <a href="/GardenTools/Create" class="btn btn-primary">Add Tool</a>
            </div>
        </div>
        <table class="table table-bordered table-hover">
            <thead class="table-secondary">
                <tr>
                    <th>Tool Name</th>
                    <th>Description</th>
                    <th style="width: 150px;" class="text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model) {
                    <tr>
                        <td class="align-middle fw-bold">@item.Name</td>
                        <td class="align-middle">@item.Description</td>
                        <td class="text-center">
                            <a href="/GardenTools/Delete/@item.Id" class="btn btn-sm btn-danger" onclick="return confirm('Delete?')">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
</body>
</html>